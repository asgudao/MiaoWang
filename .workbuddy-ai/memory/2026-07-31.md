# 2026-07-31 工作日志

## 网络调试
- 诊断手机登录「网络错误」：IP 不匹配 (192.168.1.5 vs 192.168.1.3)，批量修复 9 个 .uvue 文件
- 添加 Windows 防火墙 8080 端口规则
- 切换手机热点方案，IP 变为 192.168.178.183，再次批量替换
- 关键发现：Windows 连新网络时默认「公用」→ 改「专用」后网络通信正常

## CSS 兼容性修复
- **根因**：uni-app x 使用原生渲染，不支持 CSS `linear-gradient`、`box-shadow`、`letter-spacing`
- 批量清理所有 9 个 .uvue 页面中的不兼容 CSS 属性
- 渐变背景替换为对应纯色，阴影全部删除
- 登录按钮不显示文字问题：渐变背景在原生层不渲染 → 替换为 `background-color: #FF8C42`

## 部署
- 通过 adb 完成前端文件推送 + App 热重启
